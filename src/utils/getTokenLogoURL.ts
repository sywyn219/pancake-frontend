const getTokenLogoURL = (address: string) =>
   `https://nftmint.info/images/tokens/${address}.png`
  // `https://assets.trustwalletapp.com/blockchains/smartchain/assets/${address}/logo.png`

export default getTokenLogoURL
